# Local deployment config (copy to .deploy.env and fill in your values)
# This file is used by 00-all-deploy.ps1 to SSH into the server.

DEPLOY_SSH_HOST=你的服务器IP
DEPLOY_SSH_USER=root
DEPLOY_SERVER_DIR=/opt/leadership-factory
